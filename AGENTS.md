# AGENTS.md — AI 代理行为准则与技能配置入口

> 本文件是项目 AI 代理（Agent）的行为准则、失败处理规则与技能配置的统一入口。
> 所有 AI 代理在介入本项目前，必须首先阅读并遵循本文件约定。

---

## 一、行为准则：karpathy-guidelines

本项目采用 Andrej Karpathy 四条核心编码准则（karpathy-guidelines）作为 AI 代理的行为基准。
所有准则按优先级从上到下执行，发生冲突时以上方准则为准。

### 准则一：先思考再编码（Think Before You Code）

**核心思想**：编码前必须完成设计文档编写与方案评审，杜绝"边想边写"。

**执行要求**：
- 接到任务后，先输出设计方案（含目标、范围、接口契约、数据流、风险点），再进入编码阶段。
- 涉及多模块改动时，必须先在 `docs/plans/` 下产出实施计划文档。
- 设计方案需明确"做什么 / 不做什么"的边界，避免范围蔓延。
- 复杂决策需对照 `docs/architecture/challenges.md` 中的既有难点方案。

**检查清单**：
- [ ] 是否已明确本次变更的目标与验收标准？
- [ ] 是否已产出设计文档或实施计划？
- [ ] 是否已评估对现有模块的影响范围？
- [ ] 是否已识别潜在风险并给出应对方案？
- [ ] 是否已确认无需修改的部分保持不变？

### 准则二：简单优先（Simplicity First）

**核心思想**：在所有可行方案中，选择最简单且能解决问题的方案。

**执行要求**：
- 优先使用标准库与项目既有依赖，避免引入新依赖。
- 禁止为一次性操作创建抽象层、工厂或配置体系。
- 禁止"以防万一"式的防御性代码与错误处理。
- 复杂逻辑优先拆分为可独立验证的小函数，而非堆砌条件分支。

**检查清单**：
- [ ] 是否存在更简单的实现方式？
- [ ] 是否引入了非必要的新依赖？
- [ ] 是否为未来"可能"的需求预留了过度设计？
- [ ] 代码是否可被一名新成员在 5 分钟内读懂？
- [ ] 是否复用了项目既有工具函数而非重复实现？

### 准则三：精准修改（Precise Modifications）

**核心思想**：只做必要的修改，保持变更最小化，不夹带"顺手改进"。

**执行要求**：
- 每次变更严格限定在任务范围内，不重构未涉及的代码。
- 不为未修改的代码添加注释、文档字符串或类型标注。
- 不调整与本次任务无关的代码格式与导入顺序。
- 变更需可被一次 Code Review 完整审阅，避免大范围散弹式修改。

**检查清单**：
- [ ] 本次 diff 是否仅包含任务所需改动？
- [ ] 是否夹带了格式化、重命名等无关变更？
- [ ] 是否修改了与任务无关的文件？
- [ ] 是否为未改动代码添加了多余注释或类型标注？
- [ ] 变更是否可被一次评审完整覆盖？

### 准则四：目标驱动（Goal-Driven）

**核心思想**：始终以最终目标为导向，避免陷入工具或过程的细节泥潭。

**执行要求**：
- 每一步操作都需能回答"这如何推进最终目标"。
- 遇到阻塞时，优先评估是否可绕过非关键障碍推进主线，而非死磕。
- 完成任务后需对照目标做交付验证，而非仅以"代码写完"为终点。
- 遇到模糊需求时，主动澄清目标，而非自行假设后大规模实施。

**检查清单**：
- [ ] 当前操作是否直接服务于最终目标？
- [ ] 是否存在偏离目标的"镀金"行为？
- [ ] 完成后是否对照目标做了交付验证？
- [ ] 遇到模糊点是否已澄清而非自行假设？
- [ ] 是否存在可推迟到下一迭代的非关键工作？

---

## 二、失败处理规则

为保证 AI 代理行为可控、可追溯，执行以下失败处理规则：

1. **三次失败终止原则**：任何单一操作（如编译、测试、修复某个 Bug、调用某个工具）连续尝试 **3 次** 仍未成功时，**立即终止**该操作链路，禁止无限重试。
2. **问题记录**：终止后，将问题详情记录至问题清单（`docs/error-handling/iteration-errors-500.md` 或对应迭代错误文档），记录内容包括：
   - 操作描述与目标
   - 三次尝试的具体做法与各自失败原因
   - 最终错误信息（日志、堆栈、退出码）
   - 已排除的可能性与待排查方向
3. **上报与等待**：记录完成后，将问题摘要上报，等待人工介入或新指令，禁止在未获新指令前自行尝试新方案。
4. **禁止行为**：
   - 禁止通过"重试相同命令"碰运气。
   - 禁止隐瞒失败、伪造成功结果。
   - 禁止绕过失败以"部分完成"伪装交付。

---

## 三、技能配置索引

本项目 AI 代理按以下顺序加载与使用技能。`karpathy-guidelines` 为最高优先级，必须最先配置。

### 3.1 核心行为准则（优先配置）

| 技能名称 | 说明 | 配置状态 |
|---------|------|---------|
| `karpathy-guidelines` | Andrej Karpathy 四条核心编码准则，见本文档第一章 | ✅ 已配置（本文件） |

### 3.2 项目开发技能

| 技能名称 | 文档路径 | 说明 |
|---------|---------|------|
| Go 后端开发技能 | `docs/skills/golang-development.md` | Go 1.24+ + Gin 后端开发指南 |
| Vue 3 前端开发技能 | `docs/skills/vue3-development.md` | Vue 3 + TypeScript + Vite 前端开发指南 |
| 测试技能 | `docs/skills/testing-skills.md` | 单元测试、集成测试、性能测试规范 |
| 代码审查技能 | `docs/skills/code-review-skills.md` | 代码审查流程与检查清单 |

### 3.3 GStack 技能集

GStack 技能集覆盖从规划到交付的完整研发流程：

| 技能名称 | 阶段 | 说明 |
|---------|------|------|
| `office-hours` | 规划 | 办公时间式需求澄清与方案讨论 |
| `plan-ceo-review` | 规划 | 计划的 CEO 视角评审（业务价值） |
| `plan-eng-review` | 规划 | 计划的工程视角评审（技术可行性） |
| `plan-design-review` | 规划 | 计划的设计视角评审（架构与体验） |
| `review` | 执行 | 代码审查执行 |
| `ship` | 交付 | 发布与交付流程 |
| `cso` | 治理 | 首席安全官视角的安全审查 |
| `qa` | 验证 | 质量保证与验收测试 |

### 3.4 Superpower 技能集

Superpower 技能集提供端到端的高质量研发工作流：

| 技能名称 | 说明 |
|---------|------|
| `brainstorming` | 创意构思与需求探索（任何创造性工作前必须使用） |
| `writing-plans` | 编写实施计划（多步骤任务动代码前使用） |
| `test-driven-development` | 测试驱动开发（实现功能或修复前使用） |
| `requesting-code-review` | 请求代码审查 |
| `finishing-a-development-branch` | 完成开发分支（合并前收尾） |
| `systematic-debugging` | 系统化调试（排查复杂问题） |
| `verification-before-completion` | 完成前验证（交付前必须执行） |

### 3.5 Caveman 模式

Caveman 模式为高 Token 效率通信模式，配置文件位于 `.trae/caveman-config.json`。

| 强度等级 | 最大响应行数 | 最大代码行数 | 仅代码 | 无解释 | 适用场景 |
|---------|------------|------------|-------|-------|---------|
| `lite` | 10 | 50 | 否 | 否 | 日常轻量交互 |
| `full` | 5 | 30 | 是 | 否 | 默认强度，常规开发 |
| `ultra` | 3 | 15 | 是 | 是 | 极端 Token 受限场景 |

**自动触发条件**：Token 用量 > 50000、对话轮次 > 20、或用户输入 `/caveman`、`caveman mode`、`use caveman`、`less tokens`、`be brief`。
**退出命令**：`/normal`、`/standard`、`normal mode`、`standard mode`。

### 3.6 PUA 技能

PUA 技能为 AI 代理的高强度执行与自驱模式，配置文件位于 `.trae/pua-config.json`。

| 强度等级 | 说明 | 适用场景 |
|---------|------|---------|
| `lite` | 轻度自驱，关键节点主动推进 | 日常开发辅助 |
| `full` | 全程自驱，主动识别并完成任务 | 独立承担模块开发 |
| `ultra` | 极致自驱，端到端自主交付 | 无人值守的批量任务 |

---

## 四、项目核心要点

- **后端技术栈**：Go 1.24+，Gin Web 框架，Excelize 库
- **前端技术栈**：Vue 3+，TypeScript，Vite
- **架构模式**：前后端分离，API 路径前缀 `/api/v1`
- **分层架构**：`Entity → Repository → Service → Handler`
- **依赖注入**：使用 `wire` 进行依赖注入（参考 `docs/wire-usage.md`）
- **LLM-wiki 架构**：采用 Andrej Karpathy 的 LLM-wiki 三层架构思想组织知识库
- **分支管理**：开发分支 `dev`，主分支需审核后合并

---

## 五、开发规则

### 5.1 先思考后开发原则

- 遵循 karpathy-guidelines 准则一，编码前必须完成设计文档与方案评审。
- 遵循"文档先行"原则，所有功能开发先在 `docs/` 下产出对应文档。
- 复杂任务必须先调用 `brainstorming` 与 `writing-plans` 技能产出计划。

### 5.2 代码推送规则

- 所有代码提交必须推送到 `dev` 分支，**禁止直接推送到主分支**。
- 主分支合并需经过 Code Review（`review` 技能）与 CI 流水线通过。
- 提交信息遵循 Conventional Commits 规范（项目已配置 commitlint）。

### 5.3 文档管理规则

- 所有项目文档统一存放在 `docs/` 目录下，禁止散落在仓库根目录或代码目录中。
- 文档分类与目录结构详见 `docs/README.md`。
- 编号文档（01-15）按研发流程顺序组织，覆盖从需求到交付的全生命周期。

### 5.4 代码质量规则

- 前端：无 TypeScript 错误，构建成功，遵循 ESLint + Prettier 规范。
- 后端：遵循 `.golangci.yml` 规范，所有 API 接口保持前后端一致性。
- 测试：新功能必须配套测试，遵循 `test-driven-development` 技能。

---

## 六、文档索引（01-15 编号文档）

编号文档按研发流程顺序组织，位于 `docs/` 目录下：

| 编号 | 文档 | 说明 | 状态 |
|-----|------|------|------|
| 01 | `docs/01_requirements_analysis.md` | 需求分析文档 | ✅ 已存在 |
| 02 | `docs/02_requirements_review.md` | 需求评审文档 | ✅ 已存在 |
| 03 | `docs/03_implementation_plan.md` | 实施计划文档 | ✅ 已存在 |
| 04 | `docs/04_test_plan.md` | 测试计划文档 | ✅ 已存在 |
| 05 | `docs/05_code_review.md` | 代码审查文档 | ✅ 已存在 |
| 06 | `docs/06_iteration_plan.md` | 迭代计划文档 | ✅ 已存在 |
| 07 | `docs/07_task_list.md` | 任务清单文档 | ✅ 已存在 |
| 08 | `docs/08_deployment_guide.md` | 部署指南文档 | ⏳ 待创建 |
| 09 | `docs/09_api_documentation.md` | API 接口文档 | ⏳ 待创建 |
| 10 | `docs/10_database_design.md` | 数据库设计文档 | ⏳ 待创建 |
| 11 | `docs/11_architecture_design.md` | 架构设计文档 | ⏳ 待创建 |
| 12 | `docs/12_security_audit.md` | 安全审计文档 | ⏳ 待创建 |
| 13 | `docs/13_performance_optimization.md` | 性能优化文档 | ⏳ 待创建 |
| 14 | `docs/14_operations_manual.md` | 运维手册文档 | ⏳ 待创建 |
| 15 | `docs/15_project_summary.md` | 项目总结文档 | ⏳ 待创建 |

> 注：08-15 为规划中的编号文档，按研发流程推进逐步创建。已存在的相关主题文档（如 `architecture.md`、`api-documentation.md`、`database-design.md` 等）将逐步整合归入编号体系。

---

## 七、变更记录表

| 版本 | 日期 | 变更内容 | 变更人 |
|-----|------|---------|-------|
| v1.0 | 2026-06-17 | 初始创建：建立 AI 代理行为准则、失败处理规则、技能配置索引、项目核心要点、开发规则、文档索引 | AI Agent |
