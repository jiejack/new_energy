#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."

cd web

# Run lint-staged
npx lint-staged

# Run type check
npm run typecheck

echo "Pre-commit checks passed!"
