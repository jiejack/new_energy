apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "new-energy-monitoring.fullname" . }}-alarm-service
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "new-energy-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: alarm-service
spec:
  replicas: {{ .Values.alarmService.replicaCount }}
  selector:
    matchLabels:
      {{- include "new-energy-monitoring.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: alarm-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "new-energy-monitoring.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: alarm-service
        {{- with .Values.alarmService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.alarmService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.alarmService.service.port }}"
        prometheus.io/path: "/metrics"
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "new-energy-monitoring.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: alarm-service
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.global.imageRegistry }}{{ .Values.alarmService.image.repository }}:{{ .Values.alarmService.image.tag }}"
          imagePullPolicy: {{ .Values.alarmService.image.pullPolicy | default .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.alarmService.service.targetPort }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            {{- with .Values.alarmService.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          envFrom:
            - configMapRef:
                name: {{ .Values.alarmService.configFrom.configMapRef | default (printf "%s-config" (include "new-energy-monitoring.fullname" .)) }}
            - secretRef:
                name: {{ .Values.alarmService.configFrom.secretRef | default (printf "%s-secrets" (include "new-energy-monitoring.fullname" .)) }}
          {{- if .Values.alarmService.healthCheck.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.alarmService.healthCheck.liveness.path }}
              port: {{ .Values.alarmService.healthCheck.liveness.port }}
            initialDelaySeconds: {{ .Values.alarmService.healthCheck.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.alarmService.healthCheck.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.alarmService.healthCheck.liveness.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.alarmService.healthCheck.liveness.failureThreshold | default 3 }}
          {{- end }}
          {{- if .Values.alarmService.healthCheck.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.alarmService.healthCheck.readiness.path }}
              port: {{ .Values.alarmService.healthCheck.readiness.port }}
            initialDelaySeconds: {{ .Values.alarmService.healthCheck.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.alarmService.healthCheck.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.alarmService.healthCheck.readiness.timeoutSeconds | default 3 }}
            failureThreshold: {{ .Values.alarmService.healthCheck.readiness.failureThreshold | default 3 }}
          {{- end }}
          resources:
            {{- toYaml .Values.alarmService.resources | nindent 12 }}
      {{- with .Values.alarmService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.alarmService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.alarmService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds | default 30 }}
      priorityClassName: {{ .Values.priorityClassName | default "" }}
