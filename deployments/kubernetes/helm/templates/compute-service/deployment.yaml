apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "new-energy-monitoring.fullname" . }}-compute-service
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "new-energy-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: compute-service
spec:
  replicas: {{ .Values.computeService.replicaCount }}
  selector:
    matchLabels:
      {{- include "new-energy-monitoring.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: compute-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "new-energy-monitoring.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: compute-service
        {{- with .Values.computeService.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.computeService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.computeService.service.port }}"
        prometheus.io/path: "/metrics"
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "new-energy-monitoring.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: compute-service
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.global.imageRegistry }}{{ .Values.computeService.image.repository }}:{{ .Values.computeService.image.tag }}"
          imagePullPolicy: {{ .Values.computeService.image.pullPolicy | default .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.computeService.service.targetPort }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            {{- with .Values.computeService.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          envFrom:
            - configMapRef:
                name: {{ .Values.computeService.configFrom.configMapRef | default (printf "%s-config" (include "new-energy-monitoring.fullname" .)) }}
            - secretRef:
                name: {{ .Values.computeService.configFrom.secretRef | default (printf "%s-secrets" (include "new-energy-monitoring.fullname" .)) }}
          {{- if .Values.computeService.healthCheck.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.computeService.healthCheck.liveness.path }}
              port: {{ .Values.computeService.healthCheck.liveness.port }}
            initialDelaySeconds: {{ .Values.computeService.healthCheck.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.computeService.healthCheck.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.computeService.healthCheck.liveness.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.computeService.healthCheck.liveness.failureThreshold | default 3 }}
          {{- end }}
          {{- if .Values.computeService.healthCheck.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.computeService.healthCheck.readiness.path }}
              port: {{ .Values.computeService.healthCheck.readiness.port }}
            initialDelaySeconds: {{ .Values.computeService.healthCheck.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.computeService.healthCheck.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.computeService.healthCheck.readiness.timeoutSeconds | default 3 }}
            failureThreshold: {{ .Values.computeService.healthCheck.readiness.failureThreshold | default 3 }}
          {{- end }}
          resources:
            {{- toYaml .Values.computeService.resources | nindent 12 }}
      {{- with .Values.computeService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.computeService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.computeService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds | default 30 }}
      priorityClassName: {{ .Values.priorityClassName | default "" }}
