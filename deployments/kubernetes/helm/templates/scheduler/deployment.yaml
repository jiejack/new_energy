apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "new-energy-monitoring.fullname" . }}-scheduler
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "new-energy-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler
spec:
  replicas: {{ .Values.scheduler.replicaCount }}
  selector:
    matchLabels:
      {{- include "new-energy-monitoring.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        {{- include "new-energy-monitoring.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: scheduler
        {{- with .Values.scheduler.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.scheduler.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.scheduler.service.port }}"
        prometheus.io/path: "/metrics"
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "new-energy-monitoring.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: scheduler
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.global.imageRegistry }}{{ .Values.scheduler.image.repository }}:{{ .Values.scheduler.image.tag }}"
          imagePullPolicy: {{ .Values.scheduler.image.pullPolicy | default .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.scheduler.service.targetPort }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            {{- with .Values.scheduler.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          envFrom:
            - configMapRef:
                name: {{ .Values.scheduler.configFrom.configMapRef | default (printf "%s-config" (include "new-energy-monitoring.fullname" .)) }}
            - secretRef:
                name: {{ .Values.scheduler.configFrom.secretRef | default (printf "%s-secrets" (include "new-energy-monitoring.fullname" .)) }}
          {{- if .Values.scheduler.healthCheck.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.scheduler.healthCheck.liveness.path }}
              port: {{ .Values.scheduler.healthCheck.liveness.port }}
            initialDelaySeconds: {{ .Values.scheduler.healthCheck.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.scheduler.healthCheck.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.scheduler.healthCheck.liveness.timeoutSeconds | default 5 }}
            failureThreshold: {{ .Values.scheduler.healthCheck.liveness.failureThreshold | default 3 }}
          {{- end }}
          {{- if .Values.scheduler.healthCheck.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.scheduler.healthCheck.readiness.path }}
              port: {{ .Values.scheduler.healthCheck.readiness.port }}
            initialDelaySeconds: {{ .Values.scheduler.healthCheck.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.scheduler.healthCheck.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.scheduler.healthCheck.readiness.timeoutSeconds | default 3 }}
            failureThreshold: {{ .Values.scheduler.healthCheck.readiness.failureThreshold | default 3 }}
          {{- end }}
          resources:
            {{- toYaml .Values.scheduler.resources | nindent 12 }}
      {{- with .Values.scheduler.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scheduler.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scheduler.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds | default 30 }}
      priorityClassName: {{ .Values.priorityClassName | default "" }}
